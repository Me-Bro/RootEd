node_modules
.pnpm-store
dist
build
.env
.env.local
.env.*.local
coverage
*.log
.DS_Store
Thumbs.db
cookies.txt

# Playwright
apps/web/playwright-report/
apps/web/test-results/
apps/web/tests/fixtures/.auth/
apps/web/tests/seed/.snapshot/
apps/web/tests/seed/.test-ids.json

.claude/settings.local.json

# Private local notes/docs, not for the repo — keep the directory itself in
# git (so it exists after a fresh clone) but ignore everything inside it
private-doc/*
!private-doc/.gitkeep

# Locally-generated TLS material — never commit private keys
nginx/certs/
