# Test-mode env — loaded by `vite --mode test`
# API calls go through the Vite proxy (/__api), which forwards to localhost:3001
# with Host: testschool.localhost so tenant resolution works.
VITE_API_URL=http://127.0.0.1:5173/__api
VITE_APP_DOMAIN=localhost
