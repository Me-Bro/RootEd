VITE_API_URL=http://localhost:3001
VITE_APP_DOMAIN=rooted.app
VITE_PORTAL_SUBDOMAIN=
VITE_SENTRY_DSN=
# Google OAuth client id (same value as the API's GOOGLE_CLIENT_ID). Blank
# hides the "Continue with Google" button rather than showing a broken one.
VITE_GOOGLE_CLIENT_ID=
